title: Home Page

extends: pages/default

columns:
  main:
    fields:
      text:
        label: Text
        type: writer
        nodes:
          - heading
          - paragraph
        headings:
          - 3
        marks: false